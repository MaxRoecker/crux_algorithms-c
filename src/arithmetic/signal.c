#include "./signal.h"




inline Bool CRUX__is_positive_char (const Char value) {
  const Bool is_positive = CRUX__is_greater_char(value, as_char(0));
  return is_positive;
}

inline Bool CRUX__is_positive_size (const Size value) {
  const Bool is_positive = CRUX__is_greater_size(value, as_size(0));
  return is_positive;
}

inline Bool CRUX__is_positive_iu08 (const IU08 value) {
  const Bool is_positive = CRUX__is_greater_iu08(value, as_iu08(0));
  return is_positive;
}

inline Bool CRUX__is_positive_iu16 (const IU16 value) {
  const Bool is_positive = CRUX__is_greater_iu16(value, as_iu16(0));
  return is_positive;
}

inline Bool CRUX__is_positive_iu32 (const IU32 value) {
  const Bool is_positive = CRUX__is_greater_iu32(value, as_iu32(0));
  return is_positive;
}

inline Bool CRUX__is_positive_iu64 (const IU64 value) {
  const Bool is_positive = CRUX__is_greater_iu64(value, as_iu64(0));
  return is_positive;
}

inline Bool CRUX__is_positive_is08 (const IS08 value) {
  const Bool is_positive = CRUX__is_greater_is08(value, as_is08(0));
  return is_positive;
}

inline Bool CRUX__is_positive_is16 (const IS16 value) {
  const Bool is_positive = CRUX__is_greater_is16(value, as_is16(0));
  return is_positive;
}

inline Bool CRUX__is_positive_is32 (const IS32 value) {
  const Bool is_positive = CRUX__is_greater_is32(value, as_is32(0));
  return is_positive;
}

inline Bool CRUX__is_positive_is64 (const IS64 value) {
  const Bool is_positive = CRUX__is_greater_is64(value, as_is64(0));
  return is_positive;
}




inline Bool CRUX__is_negative_char (const Char value) {
  const Bool is_negative = CRUX__is_less_char(value, as_char(0));
  return is_negative;
}

inline Bool CRUX__is_negative_size (const Size value) {
  const Bool is_negative = CRUX__is_less_size(value, as_size(0));
  return is_negative;
}

inline Bool CRUX__is_negative_iu08 (const IU08 value) {
  const Bool is_negative = CRUX__is_less_iu08(value, as_iu08(0));
  return is_negative;
}

inline Bool CRUX__is_negative_iu16 (const IU16 value) {
  const Bool is_negative = CRUX__is_less_iu16(value, as_iu16(0));
  return is_negative;
}

inline Bool CRUX__is_negative_iu32 (const IU32 value) {
  const Bool is_negative = CRUX__is_less_iu32(value, as_iu32(0));
  return is_negative;
}

inline Bool CRUX__is_negative_iu64 (const IU64 value) {
  const Bool is_negative = CRUX__is_less_iu64(value, as_iu64(0));
  return is_negative;
}

inline Bool CRUX__is_negative_is08 (const IS08 value) {
  const Bool is_negative = CRUX__is_less_is08(value, as_is08(0));
  return is_negative;
}

inline Bool CRUX__is_negative_is16 (const IS16 value) {
  const Bool is_negative = CRUX__is_less_is16(value, as_is16(0));
  return is_negative;
}

inline Bool CRUX__is_negative_is32 (const IS32 value) {
  const Bool is_negative = CRUX__is_less_is32(value, as_is32(0));
  return is_negative;
}

inline Bool CRUX__is_negative_is64 (const IS64 value) {
  const Bool is_negative = CRUX__is_less_is64(value, as_is64(0));
  return is_negative;
}




inline Bool CRUX__is_zero_char (const Char value) {
  const Bool is_zero = CRUX__is_equal_char(value, as_char(0));
  return is_zero;
}

inline Bool CRUX__is_zero_size (const Size value) {
  const Bool is_zero = CRUX__is_equal_size(value, as_size(0));
  return is_zero;
}

inline Bool CRUX__is_zero_iu08 (const IU08 value) {
  const Bool is_zero = CRUX__is_equal_iu08(value, as_iu08(0));
  return is_zero;
}

inline Bool CRUX__is_zero_iu16 (const IU16 value) {
  const Bool is_zero = CRUX__is_equal_iu16(value, as_iu16(0));
  return is_zero;
}

inline Bool CRUX__is_zero_iu32 (const IU32 value) {
  const Bool is_zero = CRUX__is_equal_iu32(value, as_iu32(0));
  return is_zero;
}

inline Bool CRUX__is_zero_iu64 (const IU64 value) {
  const Bool is_zero = CRUX__is_equal_iu64(value, as_iu64(0));
  return is_zero;
}

inline Bool CRUX__is_zero_is08 (const IS08 value) {
  const Bool is_zero = CRUX__is_equal_is08(value, as_is08(0));
  return is_zero;
}

inline Bool CRUX__is_zero_is16 (const IS16 value) {
  const Bool is_zero = CRUX__is_equal_is16(value, as_is16(0));
  return is_zero;
}

inline Bool CRUX__is_zero_is32 (const IS32 value) {
  const Bool is_zero = CRUX__is_equal_is32(value, as_is32(0));
  return is_zero;
}

inline Bool CRUX__is_zero_is64 (const IS64 value) {
  const Bool is_zero = CRUX__is_equal_is64(value, as_is64(0));
  return is_zero;
}

